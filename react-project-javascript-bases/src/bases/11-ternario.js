const activo = true;

/* let mensaje;
if (activo) {
  mensaje = "Activo";
} else {
  mensaje = "Inactivo";
} */

// Operador ternario (IF ELSE)
// const mensaje = activo ? "Activo" : "Inactivo";

// Operador ternario corto solo como (IF)
const mensaje = activo && "Activo";

console.log(mensaje);
